(function (Drupal) {
  var ce_etherpad = {
    attach: function attach(element, format) {},
    detach : function detach(element, format, trigger){},
    onChange: function onChange(element, callback) {}
  };
  Drupal.editors.ce_etherpad = ce_etherpad;
})(Drupal);
